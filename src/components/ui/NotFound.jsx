const NotFound = ({ title }) => {
  return (
    <div className="h-full min-h-[50vh] bg-gray-100 rounded-2xl flex items-center justify-center">
      <span className="text-2xl text-gray-600 font-medium">{title}</span>
    </div>
  );
};

export default NotFound;
